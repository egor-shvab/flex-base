node_modules
.nuxt
.output
dist
package-lock.json

# The generated Prisma client. Also covered by .gitignore (which Prettier reads by
# default), but stated here so the intent does not depend on that.
server/generated
